---
layout: post
title: linux下统计文件和文件夹数
categories: [编程, linux]
tags: [linux, ls]
---

```
统计文件数
ls -l |grep "^-"|wc -l

统计目录数
ls -l |grep "^ｄ"|wc -l

统计文件数，包括子文件夹里的
ls -lR|grep "^-"|wc -l

统计目录数，包括子文件夹里的
ls -lR|grep "^d"|wc -l
```